[package]
name = "rspacefs-core"
description = "Pure-Rust userspace OverlayFS: layered read-only lowers + writable upper, OCI whiteouts, copy-up on write. vfs::FileSystem impl, no kernel, no async."
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[lib]
name = "rspacefs_core"
path = "src/lib.rs"

[dependencies]
vfs = { workspace = true }
